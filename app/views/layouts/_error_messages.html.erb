<% if @post.errors.any? %>
  <div class="form__errors">
    <h3><%= @post.errors.count %>件のエラーが発生しました</h3>
    <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>